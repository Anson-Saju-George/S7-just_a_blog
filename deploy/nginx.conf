server {
    listen 8000;
    server_name _;
    root /usr/share/nginx/html;
    absolute_redirect off;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location = /healthz {
        default_type text/plain;
        return 200 "ok\n";
    }
}
